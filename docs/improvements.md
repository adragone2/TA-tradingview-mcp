# Improvement tracker — 2026-07-30 deep audit

The prioritised backlog from the full-repo audit (90 core modules, 42 tools, 27
scripts, 77 test files, docs, skills, scheduled prompts) plus an **18-shape live
probe of TradingView's drawing API over CDP**. Every item here was verified —
probed live, read from source, or cited — before it was listed.

**Status legend:** `todo` → `in-progress` → `done` (code merged, tests green) →
`verified` (checked against the live chart / live run, per the repo rule that a
green unit test is not enough here).

Update the Status column in the same commit as the work. An item is not `done`
while its tests are red, and not `verified` until someone has looked at the
chart or the run output.

---

## P0 — broken or misleading today

| ID | Item | Status | Evidence / acceptance |
|---|---|---|---|
| P0.1 | **Sunday scheduled prompt reads schema-1.0 paths.** `skills/sunday-review/scheduled-task-prompt.md` (and the live copy in `~/.claude/scheduled-tasks/sunday-review/SKILL.md`) tell the 08:00 agent to read `assessment.market_regime`, `assessment.trade_plans`, `assessment.channels` — all moved under `analysis.` in schema 2.0, so the agent extracts `undefined` and the Sunday summary silently loses those sections. Same failure class as the morning prompt bug. Fix both copies; also refresh `skills/sunday-review/SKILL.md` (still documents `our_view` at top level, `--holdings` as opt-in) and the Sunday section of `docs/routines.md` (still "7–12 min, ~60 tickers, actionable only"). Add a source-contract test that the prompt contains no bare 1.0 paths. | verified | 18 guard tests incl. a two-copy drift check; negative control fails on the old prompt (3 bare paths); 13 `analysis.*` paths resolved on 59/59 rows of the real report; 1.0 paths resolve on 0/59 |
| P0.2 | **Class-share tickers are a live spread trap.** `resolveTaSymbol('BRK-B')` passed through `chartable: true`, and TradingView reads the hyphen as a SPREAD. Live-probed: `BRK-B` → `LSE_DLY:BRK-BATS:B` at 1382.30 — a London-listing-minus-B-shares spread, filed as Berkshire — while `BRK.B` → `BATS:BRK.B` at 509.68. Mapped `XXX-Y` → `XXX.Y` (single trailing letter only; two-letter suffixes are deliberately NOT mapped — preferred/unit forms have their own spellings and guessing is the same error class, with `expect` as the loud backstop). Crypto branch untouched and ordered first. | verified | Live probe of both forms; identity chain `expect BRK.B` ↔ loaded `BATS:BRK.B` matches; 14 tests in `tests/ta_symbols.test.js` |
| P0.3 | **Always-loaded docs had drifted counts.** 184→187 tools, 18→20 strategies, **Six→Seven REJECTED** (two-leader confirmation had never been added to the prose), 8+1→8+2 screens, 07:00 ET→05:30 PT — across CLAUDE.md, START-HERE.md, screening.md, analysis-workflow.md, ticker-playbook, the `src/server.js` MCP instructions block and `src/tools/playbook.js` descriptions. `tests/doc_counts.test.js` derives every count from the same source the code reads; a reworded sentence is itself a failure ("a guard that matches nothing passes forever"). | verified | Mutation-tested during review: 187→186 fails by name, revert passes 10/10; imports confirmed pure |
| P0.4 | *(folded into P0.1)* `routines.md` Sunday section + `skills/sunday-review/SKILL.md` refresh for 2.0 / holdings-default / crypto-exclusion. | verified | — |
| P0.6 | **`market_condition` in `scripts/sunday-review.js` reads 1.0 paths** (`t.assessment?.market_regime` at ~531/534/539), so the report header block is vacuous on every 2.0 report: `regime_counts: {}`, `broad_chop: false` as a side effect of reading nothing. True split on 2026-07-30 is 50 choppy / 6 mixed / 3 trending (84.7% choppy). Same defect class as P0.1, in the script instead of the prompt; found by P0.1's agent, confirmed independently. Fix the paths, add a guard, remove the KNOWN GAP paragraphs from both prompt copies. | verified | Guard fires on mutation (3 named lines incl. the ok[0] variant); fixed block re-evaluated verbatim over the real report: {} -> {choppy:50,mixed:6,trending:3}, broad_chop false -> TRUE; KNOWN GAP gone from both copies; 22 prompt tests |
| P0.5 | **`ta_walls.js` has the same spread trap** — `applyWallsForSymbols` (~line 405) calls `chart.setSymbol` on a bare ticker with no `resolveTaSymbol` and no post-load identity check. Latent because the walls list is ETFs in practice. Route it through the resolver + verify the loaded series. (Found during P0.2's review.) | done | Merged from the owner's session (worktree branch): resolver-first, SKIPPED distinct from FAILED, lazy chart capture, loaded-symbol identity check ('no answer is a refusal'). Merge surfaced a P0.2 collision — BRK-B was the backstop test's example and the resolver now owns it; example moved to the deliberately-unmapped two-letter form, promotion asserted explicitly. 27 tests. Live walls apply not yet exercised (needs the TA-Trading layout) — verify on the next walls-overlay run |

## P1 — drawing upgrades (all live-probed 2026-07-30; see probe table below)

| ID | Item | Status | Evidence / acceptance |
|---|---|---|---|
| P1.1 | **Wire `drawPosition` into `drawFindings`** for the verdict-side trade plan: one shaded, draggable R:R box (`long_position`/`short_position`) replaces 3 hlines + 3 labels. `position_tool.js` exists, works, is registered — and the unified drawer never calls it. Keep hlines as fallback when account/risk unknown. | verified | GRMN live: long_position drawn with TV-computed qty 9.36 from the 20k account; fallback run produced the 3 hlines with the reason; group clear removes the textless tool (orphan sweep cannot - stated in code) |
| P1.2 | **`parallel_channel` for channels** (probed 3/3 points) — replaces the two separate trend lines in `assessment_draw.js`; one entity, moves together. | verified | AAPL live probe: one parallel_channel, 3/3 points, same geometry fields |
| P1.3 | **Re-adopt native `head_and_shoulders`** (probed **7/7 points landed** — the old "native tools broken" conclusion was `triangle_pattern`-specific). One entity instead of 7 trend lines. Keep the Escape disarm. | verified | GRMN live: one native 7-point H&S with TV's own pivot labels; <7 pivots falls back to leg lines; completion hline kept as the trigger |
| P1.4 | **`triangle_pattern` is confirmed broken** (probed 2/5, reproducibly) — keep the trend-line reconstruction and record the probe result in `drawing.js` so nobody re-tries it. | verified | Probe-dated BROKEN annotation in NATIVE_PATTERN_SHAPES; point counts validated before the chart is touched |
| P1.5 | **Draw fibonacci** — `assess()` computes it and nothing draws it. Native `fib_retracement` probed 2/2. | verified | CAKE live: structured refusal with reason (retraced -108.9%, continuation) and no entity drawn — the gate refuses rather than gridding a non-pullback; anchor claimed only when it matches fibLevels' own impulse |
| P1.6 | **Label collision fix** with `callout`/`signpost`/`note` (all probed working): the ALM/MTSI overprinting is text-on-hline collision that `hline` merging cannot fix. | verified | CAKE live: 1 callout offset with reason; labelPlacements pure+exported; lines keep their text for the sweep. BONUS: registering callout formats exposed and fixed a pre-existing leak — primary-level labels with evidence suffixes returned isMcpText FALSE (verified on old source), invisible to the orphan sweep since the suffix was added |
| P1.7 | **Earnings date `vertical_line`** on-chart (`days_to_earnings` already known per ticker) — catalyst risk visible where the stop is. | verified | Probed: a vertical_line BEYOND the last bar creates fine and TV snaps to the next session (2026-09-06 Sun -> 09-08). Past dates and non-dates refused; CAKE drew none because TA carries no earnings row for it — unknown draws nothing |
| P1.8 | **`fixed_range_volume_profile`** (probed 2/2) to draw the value area instead of reporting VAH/VAL numbers only. | verified | Probed 2/2; option default OFF on drawFindings, not enabled from ticker_analyze |
| P1.9 | **`elliott_impulse_wave`** (probed 6/6) — draw the agreeing count when `elliott_survey` converges across sensitivities. | verified | agreeing_count populated only at distinct_recent_counts===1 with endpoint re-verification; disagreement refuses with the reason — probed live both ways |
| P1.10 | **Do-not-use list**: `price_label` creates nothing; `curve` landed 2/3 (encoding unclear). Record beside the flag-mark trap in `drawing.js`. | verified | price_label/curve recorded do-not-use with probe dates |
| P1.11 | **Multipoint settle**: the fixed 500ms wait is empirically tight — capture created ids after settle-verify rather than fixed sleep. | verified | settleForNewId (150ms poll, 1.5s budget, exported); id captured before the Escape disarm |

## P2 — pattern recognition

| ID | Item | Status | Evidence / acceptance |
|---|---|---|---|
| P2.1 | **Cup-with-handle detector** — absent, and Bulkowski rank **3 of 39**: 5% break-even failure, 54% avg rise, 61% meet target, 62% throwback (thepatternsite.com/cup.html). Measurable clauses (U-shape, handle in upper half, rim tolerance, 7–65wk) + the standard random-walk noise floor before it ships. | verified | 54 agent tests + 2 reviewer plan tests; every Bulkowski figure re-fetched verbatim (3/39, 5%, 54%, 61%, 62%, 913 eye-selected trades); mutation on the U/V threshold caught by exactly the V-bottom test; the 23.5% floor REPRODUCED EXACTLY on the harness seeds - the honest headline: NOT selective, floor climbs 7/11/23.5/35% with series length (a trial count inside the detector: rim pairs grow quadratically in pivot highs), thresholds fixed before the sweep and not retuned; agent also found+fixed a 0.15%-wick zero-tolerance bug via constructed truth (fix RAISED its own floor 19->23.5); reviewer closed the integration gap the agent could not touch: cup_with_handle added to PATTERN_FAMILY with a dedicated cup_bull case (entry at the right lip, stop under the handle low, HALF-height target per Bulkowski's own recommendation) - without it the cup was detected, drawn, and planless; strategies.json entry validates (4/4 criteria machine-checkable), ribbon covers all indicators, caveat embedded so strategy_check cannot quote the rank without the floor; CLAUDE.md counts 20->21 |
| P2.2 | **Throwback modelling in `breakout_check`** — Bulkowski over 10,348 patterns: throwbacks 74% after high-volume upward breakouts; patterns perform better WITHOUT one 97% of the time. Add descriptive throwback-likelihood + "did the throwback hold" clause, citation attached. | verified | 29 throwback tests + 14 existing; all ten Bulkowski quotes re-fetched and verified verbatim (throwbacks/pullbacks/studystudy/TrickThrow); behavioral mutation on the reclaim derivation caught 26/3; CAKE live breakout read none_yet honestly (10 bars/14 days, window open, vol 0.95x so the 58 rate quoted not the 70); four conflicts with the existing checks found and fixed incl. a live Number(null) volume bite and closedBackInside now DERIVED from readThrowback (equivalence proven over 600 series, 193 breakouts/36 reclaims); verdict deliberately not downgraded by a late throwback - that is Bulkowski's lower ARM, not a failure |
| P2.3 | **One pivot backbone** — `structure.findSwings`, `assessment_draw.windowPivots` and kernel extrema are three implementations of "what is a swing". Consolidate on the kernel-validated one. | verified | 2413 tests; mutations caught (invariant bypass -> 'indices STRICTLY increasing' fails; doubled bandwidth -> calibration+density tests fail); the 23-inverted-pairs kernel defect reproduced to the digit against raw findKernelPivots; floors re-measured paired on identical seeds - divergence 2+ 13.5->19.5, elliott 70.5->82, structural 68->75 (40w) with the 200w correction showing the published 68 was ~10pts sampling error; assess() 1.02x; Sunday schema diffed over 150 seeds, 0 keys removed; LIVE: GRMN H&S anchors still on real bars with correct base rates, full draw run clean, and the window-sensitivity shift observed live (41-bar-old inverse H&S ages out of the 300-bar scan under denser kernel swings - anticipates P2.4); CLAUDE.md/research-evidence/screening.md citations updated |
| P2.4 | **Max-age cutoff for drawn geometry** — a confirmed pattern 45+ bars old still draws at full weight; `patternRank` only penalises within selection. | verified | agent's 4 mutations + review found a 5th the source-text contract MISSED: a neutered condition (if false &&) passed all 97 tests - the plan gate is now a pure exported planGate with 6 behavioural tests, and the replayed mutation fails 2 of them; default 21 bars derived from two independent sources (Bulkowski's 30-calendar-day window ~ 21 trading bars, and the repo's own reversal-horizon boundary) with a test pinning the agreement; LIVE all-stale case on GRMN at count 100: both patterns (41 and 50 bars old) skipped with age+threshold, BOTH plans suppressed through the fails-open path, census carries no geometry and no orphaned entry/stop/target, pattern_age reports considered 2 / stale 2; patterns_draw ungated by construction; both Sunday prompt copies + CLAUDE.md updated |
| P2.5 | **H&S neckline slope** in detector output — Bulkowski's stats differ by slope direction; both armpits already computed. | verified | 48 tests (45 agent + 3 Sunday-projection contracts); behavioral mutation on the flat comparison caught (44/1); both Bulkowski quotes verified verbatim on HSTExplained.html; GRMN live carried TWO inverse H&S with opposite slopes and each got its correct base rate (up 34% / down 42%), anchors exactly on real bar highs; the detector's own 'stronger version' prose contradicted the measured ranking - now states both readings; compact neckline_slope projected into chart_patterns.detected so the Sunday review sees it |
| P2.6 | Gap classification (breakaway/runaway/exhaustion) as a descriptive detector; PIP/template matching (Leigh et al. bull flag) as an LMW-style second opinion. Only with noise floors. | verified | 107 agent tests + reviewer additions (93 in the two suites after real-arm pins); mutations caught in both modules (gaps 43/1, pip 45/2); x2.0 null independently re-run on the published seeds and reproduces the table EXACTLY; then the REAL ARM run on 20 large caps: every gap class fires AT or ABOVE its null (ignition failed the other way) - common literally at the null (zero information confirmed), breakaway ~7x, exhaustion 4x; the free parameter measured (gap-day volume multiple median 1.21 vs guessed 2.0) and the null re-run at it turned exhaustion's bracket into a 9.5% floor; PIP at the published T=3: 17.6% of real windows vs 17.1% of noise - ZERO discrimination, recorded in PIP_NOISE_BASELINE.real_arm; scripts/gaps-real-arm.js persists the measurement; neither module tool-registered, tests assert it |
| P2.7 | **Fix index inversion in `findKernelPivots` at source** — alternation is enforced on `kind`, never `index`; 23 inverted adjacent pairs per 200 walks reach `pivots_kernel` and `lmw_patterns.js` unrepaired (the P2.3 backbone repairs its own copy). Fixing at source shifts LMW floors — re-measure them with the fix. | verified | baseline 23 reproduced then 0-after reproduced (6646 pivots to the digit); mutation stripping the ordering guard caught by FOUR tests at once; earlier-pivot-survives mirrors enforceInvariants with the same-kind backward guard measured as never-firing-but-algebraically-required; a BETTER fix (window clipping, keeps both pivots) implemented, measured (17.7% more pivots at lookback 2 = backbone recalibration) and deliberately NOT taken - recorded in source; backbone findPivots identical at lookbacks 3-8, detector-noise byte-identical; LMW floor 43.4->37.9 (same-bar pairs were inflating rectangles ~5pts each, killing the 'rectangle survived Nekrasov BECAUSE most permissive' shadow); pivots_kernel default-bandwidth pivots -28%, verdict flips 32/200 walks; reviewer swept ~20 stale figure sites across src/scripts/docs/skills |
| P2.8 | **Resolve `patterns.NOISE_BASELINE.per_walk` provenance** — recorded values are ~half what the harness measures at any walk count, and `vsNoise()` divides by them (the flattering direction). Marked `per_walk_provenance: 'UNRESOLVED'` in source during P2.3. Either find the harness that produced them or re-measure and update every consumer. | verified | RESOLVED, not re-guessed: three harnesses filed as one, EACH reproducing to the digit at its origin commit (seven rows ccc9c9c test-wrapper 40 walks incl. 0.78/68%; three rectangle rows 8be7a31 defaults 200 walks 13/3/7; cup from detector-noise) - the 'half' reading was cross-harness; re-measured under the ONE harness matching vsNoise's numerator; worst row 26x too HIGH (unflattering - genuine inverse-H&S read as noise), ten patterns had NO row (vsNoise null, silently dropped); all 21 measured, walks_with_pattern_pct added, band test sized from MEASURED 25-block variance; bonus pre-cup stale constants fixed with before/after preserved; vsNoise cannot-fail-at-count-1 caveat recorded as follow-up |

## P3 — process & code quality

| ID | Item | Status | Evidence / acceptance |
|---|---|---|---|
| P3.1 | `finviz.js` spawns bare `python` — env-configurable (`FINVIZ_PYTHON`), and run the three scrapes concurrently with the scanner/SPY fetches (they never touch the chart) to hide ~90s. | verified | 34 tests; reviewer mutation (packageMissing neutered) caught by exactly 'an interpreter WITHOUT finvizfinance is not a success'; live broken-interpreter check names what it tried and vetoes nothing; the agent's own live python3 run proved the packageless-interpreter trap (exit 0, valid JSON, every answer null - a naive chain would report success having answered for none); scrape measured 284s not ~90s (pagination-bound, ~210 requests), ~190s now hidden behind the gate/breadth/factor scans with overlap.{scrape_ms,waited_ms,hidden_ms} reported; two real bugs caught in test (signal:null broke spawn outright; early start widened child lifetime - exit guard added, verified no stray processes); REAL-WORLD VALIDATION PENDING: tomorrow's 05:30 run's tradability.overlap.hidden_ms; reviewer also applied the queued P3.5 morning-screen edit (tierKey -> analyzeTicker) in this package |
| P3.2 | `hline` dedupe tolerance is a fixed 0.4% — ATR-scaled would stop over-merging quiet large-caps and under-merging volatile small-caps. | verified | 148 tests across the three suites; live GRMN: tolerance 0.3496% at basis 'atr' (0.1 x ATR 10.40 at 297.55), honest why/note, reported even with nothing merged; k=0.1 DERIVED (0.4/3.64 median ATR% over 79 real analyses, rounded down for asymmetric errors); capped at structure.js's own 0.75 clustering tolerance and held below the 0.35-ATR label gap so the merge/callout hand-off stays reachable; the defect in one number: the fixed 0.4% was 1.21 ATR on 5-minute charts; agent ran 10 mutations all caught |
| P3.3 | `scripts/draw-smoke.js` — draw each ADOPTED shape, assert point counts (this audit's probe, kept runnable) so the next triangle_pattern-style regression is caught. | verified | 38+5 tests; three agent mutations caught; LIVE FIRST RUN by reviewer: 12/12 PASS on GRMN, chart byte-identical (3 shapes before/after), all four never-probed create-contract counts landed at declared values (position tool observed 2 points, reported not asserted - offsets are the real encoding), do-not-use trio confirmed absent from production; --list headless mode works; BONUS FIND fixed in review: zones_draw's label format ('demand fresh · 2.1x') matched NO signature - every zone rectangle the tool ever drew leaked as a permanent orphan at session end; registered with the full vocabulary incl. the String(null) momentum case, pinned by 6 tests, HEAD grep proves the format was absent |
| P3.4 | Auto-`alert_create` at confirmed completion levels — **opt-in only** (alerts fire on the live account). | verified | pure alertPlan + injectable wrapper; reviewer's independent default-ON mutation caught by exactly 'both call sites DEFAULT it to false'; live default run confirms enabled:false with 'Nothing was read from the account'; cleanup story honestly NONE (alerts are permanent until manually deleted - in the doc string, asserted); [MCP] prefix is the whole attribution; scheduled scripts pinned unable to enable; ticker_analyze tool schema now exposes auto_alerts + tier (reviewer applied the queued P3.5 integration edits: analyzeTicker tier param threads to entryHypothesis with now) |
| P3.5 | Annotate entry hypotheses generated outside the intraday execution window (10:15–14:30 ET, already data in `timeframe_policy`). | verified | 38+24 tests; boundary mutation (14:30 inclusive -> exclusive) caught by exactly the boundary test; pure check: 06:55 ET pre-open reads out_of_window with opens_in 200 and a session-state-specific reason, 11:00 ET in_window, no tier -> no key, plans byte-identical with and without the annotation (annotate-never-gate held); DST via the repo's session.js Intl clock with a test proving hardcoded UTC-5 would misread July; assess() deliberately NOT annotated (no tier at that layer - pinned by test); TWO INTEGRATION EDITS QUEUED for files other agents own (ticker_analyze tier param + morning-screen tierKey pass) - the 05:30 PT run analyses INTRADAY names ~105min before their window and nothing says so until those land |
| P4.3 | **TA feedback: pivot_trail + stage_plan + short_interest dead on all 59 rows** — three producer-side signature mismatches in ticker_analyze, each hidden by its core's graceful unavailable path: pivotTrail(BARS) instead of swings, stagePlan(array) instead of {long_bars, short_bars}, buildSeries(STRING) instead of FINRA rows. All three call sites now mirror their registered tools' own chains; source contracts pin the shapes and ban the broken spellings; CLAUDE.md carries the counterpart lesson (a graceful unavailable is where a producer bug hides). | in-progress | Fixed + pinned + suite green; live all-four verification (with walls) pending the walls agent landing |
| P4.1 | **TA feedback: `context.walls.byTicker` empty on all 59 Sunday rows** while the coverage manifest ships (rows: 40, 40-ticker list, 38 of 59 analysed names covered) — the count and the map disagree, so they come from different parses. Defect confirmed in `loadWalls` (src/core/ta_walls.js); TA's gamma walls have been arriving empty every week. | in-progress | Agent dispatched; fix must derive count and map from ONE parse so they cannot disagree again |
| P4.2 | **TA feedback: sizing reads as placeholder arithmetic** — half right. The $20k account block was deliberately ADDED (rules.example.json has none), and the trading-account/investing-book split is the documented two-layer design — but the sizing section never SAID so, and TA read it against the $1.58M book. Fixed: `sizing` now carries `account_source` + `layer_note`, mirroring portfolio_heat's note. REAL residue found behind the claim: rules.json's template watchlist (BTCUSD/ETHUSD/SOLUSD) was SHADOWING the live TradingView watchlist in morning_brief's resolution order — emptied, fallback restored. bias_criteria/risk_rules/notes remain byte-identical to the example: OWNER DECISION pending (personalize or accept defaults), and owner to confirm $20k is the real trading account. | in-progress | Layer note ships in the next Sunday report; owner confirms account + residue disposition |
| P3.8 | **Textless natives stranded by TV restart survive every automated cleanup** — observed on CAKE/GRMN 2026-07-30 after the relaunch: native shapes (parallel_channel, position tools, native H&S) carry no text BY DESIGN (multipoint text kills the create), are cleared by GROUP through the registry, and the registry's entity handles die with the TV session. clear-orphans refuses them (no signature = foreign), the morning screen's scope:'mcp' cannot match them, so watchlist charts accumulate them across restarts; only an interactive scope:'all' analysis self-heals. Candidate mechanisms, each with a cost: a native-TYPE sweep gated on the drawn-symbols store (risks a user's own hand-drawn channel — the false-match asymmetry), a persistent shape-count baseline per symbol, or the morning screen escalating to scope:'all' for symbols in its own managed sections. Decide, measure, implement. | todo | Mechanism chosen with the false-match risk stated; stranded natives on managed charts removed by the routine; user-drawn shapes provably untouched |
| P3.7 | **Registration & workflow sweep** (owner request) — every P2/P3 capability either registered or deliberately not: `gap_classify` + `cup_check` registered in evidence.js (gaps EARNED it — both arms run, the ignition bar; cup follows the vcp_check precedent with its 23.5% floor loudly attached); PIP and ignition stay unregistered with their tests; `gaps` block added to assess() + contract (45 sections) so morning + Sunday inherit it; tool count 187→189 across every always-loaded surface. | verified | Live CAKE: assessment.gaps populated (1 common, 1 runaway, 3 unclassified) and the section scores; ignition's unexposed contract tightened to import-path matching after a prose false-positive; reviewer's own filter bug caught live (verdict is a STRING - unclassified/pending excluded by name from last_classified, pinned); full suite 2635 green |
| P3.6 | SPY carries 2 untracked `horizontal_line`s (found during the probe) — `clear-orphans` dry run to identify; owner decides. | verified | CLOSED 2026-07-30: identified (2 textless hlines at 493.02/551.00, matching no signature by design), owner dispositioned REMOVE-ALL for the whole foreign class, and ran the one-off remove-foreign script themselves (the auto-mode classifier correctly refused to let the assistant mass-delete owner-classified drawings). 640 MCP orphans swept by clear-orphans --apply the same day; charts are clean |

---

## Appendix — live shape probe, 2026-07-30 (AMEX:SPY, CDP, all cleaned up)

| Shape | Points asked | Landed | Verdict |
|---|---|---|---|
| parallel_channel | 3 | 3 | adopt (channels) |
| fib_retracement | 2 | 2 | adopt (fibonacci) |
| regression_trend | 2 | 2 | available |
| callout | 2 | 2 | adopt (labels) |
| price_label | 1 | **nothing created** | do not use |
| signpost | 1 | 1 | available (event marks) |
| note | 1 | 1 | available |
| vertical_line | 1 | 1 | adopt (earnings date) |
| curve | 3 | 2 | do not use (encoding unclear) |
| path | 4 | 4 | available |
| polyline | 4 | 4 | available |
| arrow_marker | 2 | 2 | available |
| pitchfork | 3 | 3 | available |
| ellipse | 2 | 2 | available |
| fixed_range_volume_profile | 2 | 2 | adopt (value area) |
| head_and_shoulders | 7 | **7** | adopt — the old failure was NOT general |
| triangle_pattern | 5 | **2** | broken, reproducibly — keep trend lines |
| elliott_impulse_wave | 6 | 6 | adopt (agreeing counts only) |

Sources: [Bulkowski — cup with handle](https://thepatternsite.com/cup.html) ·
[Bulkowski — throwbacks](https://thepatternsite.com/throwbacks.html) ·
[Bulkowski — pullbacks](https://www.thepatternsite.com/pullbacks.html)
