node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json

# Local user config / runtime state (added during audit fork prep)
rules.json
safety-check-log.json

# Local API credentials
.env

# Watchlist/state backups — local restore points, not for the repo
backups/
